(ns noumenon-site.content.install
  "Install + quickstart page. Phase 2 placeholder.")

(defn page []
  [:section
   [:div.container
    [:h1 "Install"]
    [:p "Quickstart, package managers, and LLM provider configuration. (Coming.)"]]])
