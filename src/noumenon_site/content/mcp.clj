(ns noumenon-site.content.mcp
  "MCP setup + tool list page. Phase 2 placeholder.")

(defn page []
  [:section
   [:div.container
    [:h1 "MCP"]
    [:p "Setup walkthroughs (Desktop, Code, generic) and the full tool list."]]])
