(ns noumenon-site.content.concepts.pipeline
  "Pipeline concept page. Phase 2 placeholder.")

(defn page []
  [:section
   [:div.container
    [:h1 "Pipeline"]
    [:p "Import → enrich → analyze → synthesize → benchmark."]]])
