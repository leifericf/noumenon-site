(ns noumenon-site.content.queries
  "Queries catalog page. Phase 2 placeholder.")

(defn page []
  [:section
   [:div.container
    [:h1 "Queries"]
    [:p "Generated from resources/queries/*.edn in the source repo."]]])
